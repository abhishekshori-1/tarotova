/** Owner decision, 17 September: availability may be 85%; safety and grounding do not change. */
export const MIN_PUBLICATION_RATE = 0.85;

/** Later owner decision: accept 4/5 publication on the fresh set only. */
export const MIN_FRESH_PUBLICATION_RATE = 0.8;
